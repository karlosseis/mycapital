<%= simple_form_for(@stockexchange) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.association :country %>
    <%= f.association :currency %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
