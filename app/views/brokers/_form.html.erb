<%= simple_form_for(@broker) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :buy_frequency %>

  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
