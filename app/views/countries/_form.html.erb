<%= simple_form_for(@country) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :perc_tax %>    
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
